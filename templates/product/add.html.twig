{% extends "base.html.twig" %}
{% block body %}
    {{ form_start(form) }}
    {{ form_row(form.name) }}
    <br/>
    {{ form_row(form.releaseOn) }}
    <br/>
    {{ form_end(form) }}
    <br>
    {{ dump(result) }}

{% endblock %}